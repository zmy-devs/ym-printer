# 仓库协作说明

## CodeGraph 优先

本仓库已包含 `.codegraph/` 索引。凡是需要定位、阅读或理解代码时，首先使用 CodeGraph；不要先用 `rg`、`find`、`Select-String` 或大范围打开文件来猜测实现位置。

- 优先执行：`codegraph explore "<符号名、文件名或问题>"`。
- 使用它查找符号定义、调用路径、模块依赖、事件流和动态分发关系。
- 需要查看某个文件或符号的当前源码时，在查询中直接写明名称。
- 仅当 CodeGraph 无法覆盖目标（如索引缺失、查询无结果，或查找非代码资源）时，再使用 `rg --files`、`rg` 或定向读取文件；此时应缩小搜索范围。

## 修改原则

- 先理解受影响的符号及调用方，再进行修改。
- 保持改动聚焦于当前任务，避免重构无关代码。
- 修改对象或数组时，优先直接修改目标对象，避免 React 风格的过度解构与重新赋值。
- 避免无效或过度的防御性代码，保持实现简洁、结构清晰。
- 保留用户已有的未提交修改，不执行会覆盖或丢弃现有工作的操作。
- 修改后运行与改动范围相称的格式化、类型检查或测试，并报告未能执行的验证项。

## 补充开发规则

- 需要做通用组件时，优先查看 `shadcn-vue` 是否已有可复用组件；需要二次封装时放在 `@/components`。
- 需要工具函数时，优先查看 `VueUse` 是否已有实现，避免重复造轮子。
- 封装组件前先查看项目中是否已有同类型组件，并保持调用方式与组件封装一致。
- 本项目为 `electron-vite`：渲染进程使用的库直接安装到 `devDependencies`；仅主进程使用且需要编译的库（如 `winax`）放在 `dependencies`。

## 界面组件

- 实现或调整界面时，优先使用仓库已安装的 `shadcn-vue` 组件及其既有封装。
- 只有在组件库无法满足交互或布局需求时，才新增自定义组件；新增内容应保持与现有视觉和代码风格一致。
- 不允许新增无障碍相关属性，例如 `aria-label`、`aria-*` 或 `role`。
- `components/common` 用于存放对基础组件的二次封装。
- `components/ui` 用于存放 `shadcn-vue` 组件。
- `components/features` 用于存放业务功能组件。
- `components/layout` 用于存放布局组件。

## 文档与沟通

- 面向仓库的说明、注释和提交内容应简洁、具体，并与现有语言风格保持一致。
- 报告时说明改动内容、验证方式及已知限制。
